import 'package:flutter/material.dart';

class AnonymityForm extends StatelessWidget {
  const AnonymityForm({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Name form"),
    );
  }
}
